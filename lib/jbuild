(jbuild_version 1)

(library
 ((name        ocaml_geth)
  (public_name ocaml-geth)
  (ocamlopt_flags (-g))
  (ocamlc_flags (-g))
  (libraries (batteries yojson netclient ssh-client cryptokit zarith hex bitstring))
  (preprocess (pps (bitstring.ppx)))
))
