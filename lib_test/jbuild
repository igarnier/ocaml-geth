(jbuild_version 1)

(executables
 ((names (test))
  (libraries (batteries mlparity ssh-client))))

; (rule
;  ((targets (mondrian.pdf))
;   (action (with-stdout-to ${@} (run ${test:mondrian.exe})))))

(alias
 ((name runtest)
  (deps   (test.exe))
  (action (run ${<}))))
